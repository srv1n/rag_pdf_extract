---
schema: "tusker.task/v7"
kind: "task"
id: "PDF-T-0003"
project: "rag_pdf_extract"
title: "Negative page-bbox coordinates reject real SC-judgment PDFs (2/21 corpus failures) — normalize coordinates + regression"
epic: "PDF"
status: "review"
readiness: "waiting_on_review"
priority: "p1"
risk: "medium"
size: "s"
proof_mode: "inline"
proof_status: "satisfied"
proof_required:
  - "focused_test"
  - "broad_test"
evidence_budget: 1
raw_artifacts_allowed: false
next_owner: "reviewer"
next_source: "review_policy"
next_ref: ""
next_action: "Review evidence and close or return to rework."
domains: []
gates: []
dependencies: []
evidence_required:
  - "automated_test"
created_at: "2026-07-21T14:30:00Z"
created_by: "agent:sarav"
updated_at: "2026-07-21T15:13:31Z"
updated_by: "agent:sarav"
state_rev: "sha256:c1997d755a2ea3d86af5f715b8bf095e67cce034535a151f6f2847b5f48b2e8c"
---

# PDF-T-0003 · Negative page-bbox coordinates reject real SC-judgment PDFs (2/21 corpus failures) — normalize coordinates + regression

## Intent

The RZN backend's first live 21-document ingestion drive (2026-07-21, Rung-1 of the
corpus ladder, Indian Supreme Court judgment PDFs) had **2 of 21 documents fail
`fetch_and_parse`** with:

```
pdf_page_bbox.locator.bboxes.{x,y} must be nonnegative
```

Real-world PDFs can carry page boxes with negative origins (MediaBox/CropBox not
anchored at 0,0 — common with scanner/repro output and some court-registry
digitizations). The extraction pipeline currently emits those raw coordinates in its
locator bboxes, and the backend's canonical locator contract rejects negative x/y.
The right fix is almost certainly parser-side: **normalize page-space coordinates so
emitted bbox origins are nonnegative** (translate by the page-box origin, i.e. make
coordinates page-relative), rather than loosening the backend contract — the
nonnegative invariant is what downstream citation/highlight rendering assumes.

If investigation proves the negative values originate in a layer this repo does not
own, the deliverable becomes: the minimal parser-side coordinate-space guarantee,
plus a written handback (exact producing code path, sample values) filed against the
backend.

## Where to find the failing inputs

- Corpus: R2 bucket `rzn-documents-prod`, prefix
  `tenants/public/sources/70c9e920-4919-4904-b45f-37dd9b92487d/raw/` (21 PDFs,
  37 KB–7.3 MB). Credentials: backend repo `.secrets/e2e/local.env` (`SOURCE_OBJECT_*`
  key names) — never commit or print values.
- The original failed job payloads sit in the backend serve box's dead-letter queue
  (`GET /jobs/dead-letter` on the tenant surface); evidence trail in the backend repo
  under `.tusker/scratch/AOS-T-0004/metrics.md`.
- Fastest local repro: fetch all 21 PDFs, run this repo's extraction CLI/example over
  the set — the 2 bbox failures are deterministic. Identify them by filename in the
  task record.

## Acceptance

| ID | Outcome | Proof |
|---|---|---|
| A1 | The 2 failing PDFs are identified by name and added as regression inputs — as fixtures if size-reasonable, else as minimized synthetic PDFs that reproduce the negative-origin page box (with a generator script, per the `reportlab_empty_text` fixture pattern). | Inline verification |
| A2 | Extraction emits nonnegative bbox coordinates for these inputs under a documented normalization rule (page-relative translation; no clamping that distorts geometry), with an automated test asserting the nonnegative invariant on the fixtures. | focused_test |
| A3 | The full 21-doc corpus run goes from 19/21 to 21/21 extraction success on this defect class (the remaining failure is PDF-T-0004's), or a written handback per Intent if root cause is out-of-repo. | broad_test |
| A4 | Existing extraction outputs for well-formed PDFs are unchanged (no coordinate shift on documents whose page boxes already start at 0,0). | broad_test |

## Non-goals

- No backend contract changes (the nonnegative invariant stays).
- No OCR-path work; these are text-extraction failures.
- PDF-T-0004 (text-slice materialization failure, 1/21) is a separate defect.

## Verification

| Covers | Check | Result | Notes |
|---|---|---|---|
| A1 | command: python3 tests/fixtures/pdf_page_bbox_origins/generate.py && ls tests/fixtures/pdf_page_bbox_origins | pass | Named live failures: `1.pdf` and `3. Gayatri Balasamy v ISG.pdf`; minimized generator and two regression inputs added. |
| A2 | command: cargo test --test pdf_page_bbox_origins | pass | 2 tests pass; complete-geometry translation preserves widths/heights and asserts nonnegative x/y. |
| A3 | command: cargo run --release --example pdf_batch_benchmark -- --max-tokens 350 <fetched-21-pdf-set> | pass | 21/21 parser extractions `ok`; former bbox failures have zero negative x/y origins. |
| A4 | command: cargo test -- --quiet && cargo test --test pdf_page_bbox_origins | pass | Full suite green; origin-zero fixture remains x=72,y=672 and corpus text/chunk metrics match baseline. |

## Evidence

Accepted:
- None.

Pending:
- None.
