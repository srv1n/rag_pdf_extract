---
schema: "tusker.task/v7"
kind: "task"
id: "PDF-T-0004"
project: "rag_pdf_extract"
title: "One SC-judgment PDF fails 'materialize typed canonical text slices' (1/21) — root-cause to the owning layer + regression"
epic: "PDF"
status: "review"
readiness: "waiting_on_review"
priority: "p1"
risk: "medium"
size: "s"
proof_mode: "inline"
proof_status: "satisfied"
proof_required:
  - "focused_test"
evidence_budget: 1
raw_artifacts_allowed: false
next_owner: "reviewer"
next_source: "review_policy"
next_ref: ""
next_action: "Review evidence and close or return to rework."
domains: []
gates: []
dependencies: []
evidence_required:
  - "automated_test"
created_at: "2026-07-21T14:30:00Z"
created_by: "agent:sarav"
updated_at: "2026-07-21T15:13:31Z"
updated_by: "agent:sarav"
state_rev: "sha256:5a06f085291cb72d64bd67a6f848c5d2a85f6b2112375d8a31c29eac5e14e4c0"
---

# PDF-T-0004 · One SC-judgment PDF fails "materialize typed canonical text slices" (1/21) — root-cause to the owning layer + regression

## Intent

Same 2026-07-21 backend ingestion drive as PDF-T-0003: **1 of 21** Supreme Court
judgment PDFs failed `fetch_and_parse` with:

```
Failed to materialize typed canonical text slices
```

Unlike PDF-T-0003 this error message names a downstream step (canonical text-slice
materialization), so the defect may live on either side of the parser boundary:

- **Parser-side hypothesis**: this repo's extraction emits output whose structure
  breaks slice materialization — e.g. empty/degenerate text runs, an offset/length
  inconsistency between extracted text and locator spans, a unit boundary the slicer
  can't type, or an encoding artifact.
- **Backend-side hypothesis**: extraction output is well-formed and the backend's
  slice materializer has an unhandled case.

This task's first job is the **diagnosis that picks the owning layer** — with the
actual document in hand — and its second job is the fix + regression *if* the owner
is this repo. If the owner is the backend, the deliverable is a written handback:
the offending document, the exact structural property of the parser output that
triggers the failure (with a minimal reproducing sample), and which backend
invariant it violates — enough for a backend implementer to fix without re-deriving
the diagnosis.

## Where to find the failing input

- Same corpus + credentials pointers as PDF-T-0003 (R2 `rzn-documents-prod`, prefix
  `tenants/public/sources/70c9e920-…/raw/`; backend `.secrets/e2e/local.env`
  `SOURCE_OBJECT_*` names; dead-letter queue `GET /jobs/dead-letter` on the backend
  serve box holds the failed job payload).
- Identify the document by elimination: 21 PDFs, 2 fail on PDF-T-0003's bbox class;
  this is the remaining failure. Record its filename in the task.

## Acceptance

| ID | Outcome | Proof |
|---|---|---|
| A1 | The failing PDF is identified by name; the failure is reproduced outside the live server (parser CLI or a minimal harness over extraction output), and the root cause is written down with the owning layer named. | Inline verification |
| A2 | If parser-owned: fix landed with an automated regression test on a fixture (or minimized synthetic) reproducing the structural trigger; extraction of the document succeeds. If backend-owned: handback document filed with the minimal reproducing sample and the violated invariant, referenced from this task. | focused_test |
| A3 | No behavior change for the other 20 corpus documents (goldens/suite unchanged). | focused_test |

## Non-goals

- No speculative slicer rewrites; smallest honest fix at the owning layer.
- PDF-T-0003 (negative bbox class) is separate.

## Verification

| Covers | Check | Result | Notes |
|---|---|---|---|
| A1 | manual proof: `cargo run --release --example diagnose_bbox -- <7. Puttaswamy v UoI.pdf>` + handback diagnosis | pass | 754 chunks, 766148 chars, 257420 spans, invalid_output_spans=0, invalid_bboxes=0; owning layer is backend materialization. |
| A2 | command: docs/handbacks/PDF-T-0004-puttaswamy.md | pass | Backend-owned handback names the call path, required inner error, and parser invariants; no parser fix made. |
| A3 | command: cargo test -- --quiet && 21-document parser corpus benchmark | pass | Full suite green; the other 20 documents retain baseline chunk/char metrics and parse successfully. |

## Evidence

Accepted:
- None.

Pending:
- None.
